{
  "name": "landing-pages-monorepo",
  "version": "1.0.0",
  "description": "Monorepo com múltiplas landing pages minimalistas e elegantes",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "cd portfolio-showcase && npm install && npm run build",
    "lint": "lerna run lint --parallel",
    "start": "lerna run start --parallel"
  },
  "devDependencies": {
    "lerna": "^7.0.0"
  }
}
